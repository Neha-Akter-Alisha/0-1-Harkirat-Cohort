//Conditional statements
let mode = "red";
let color;
if(mode==="dark"){
    color = "black";
}
//If we want to check multiple condition we will use else if condition
else if(mode==="pink"){
    color = "pink";
}
else if(mode==="blue"){
    color = "blue";
}
else{
    color = "white";
}
console.log(color);
if(mode==="red")
{//Here we are using only single condition
console.log(mode);
}