//Write a program prints the biggest number.
let arr = [3, 6, 2, 56, 32, 5, 89, 32];
let largest = Math.max(...arr);

console.log("The largest number is " + largest);
//The Math.max() method returns the number with the highest value.
